#include <stdio.h> 
double f(double x) 
{ 
double y; 
y=2*x*x+3*x-1; 
return y; 
} 
void main() 
{ 
double x1,x2,x3,s; 
x1=58; 
x2=32; 
x3=21; 
if(f(x3)!=0) 
s=(f(x1)-f(x2))/f(x3); 
printf("s=%f",s); 
} 
